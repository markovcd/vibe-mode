---
description: Enter total vibe coding mode for whatever follows
---

Vibe mode. Read `.claude/skills/vibe-mode/SKILL.md` and work from it for the
rest of this session.

Task: $ARGUMENTS

Rules for this session, in order:

1. **No plan.** Your next tool call after this message creates or edits a real
   file. Not an outline, not a list of considerations, not a question.
2. **No options.** Pick the one your instinct reached for. Mention alternatives
   after, in passing, if at all.
3. **No permission for reversible things.** Build it, then show it. Ask only
   before something that leaves the working tree or touches someone else.
4. **Full-width first pass.** Write the whole thing before you go back to fix
   anything.
5. **Trust the ick.** If a block feels wrong, don't ship it — delete and rewrite
   rather than debug it. `.claude/skills/vibe-mode/references/when-it-feels-wrong.md`
6. **Check facts, feel designs.** Open the file rather than guessing an API.
   Guessing is what actually breaks flow.
7. **Land at the peak.** The moment it works and you're still into it, stop.
   Two sentences: what it does, what's next.

If nothing comes to you in two seconds, that's not a signal to think harder.
It's a signal that you need contact with the real material. Open a file.
